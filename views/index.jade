extends layout

block content
  header Bucketlists
    ///search goes here///
  main
    for user in users  
      div
        a(href= '/bucketlists/'+list.id)
        p= user.display_name
        img(src= user.picture)
            p= item.title
